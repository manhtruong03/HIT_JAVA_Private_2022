package CodeTrenLop;

import java.util.ArrayList;

public class TheoryArrayList {
	// Kieu du lieu truyen vao phai la mot Wrapper class hoac Object
	
	/*
	 * Diem khac so voi Array
	 * 1. Khong can khai bao truoc kieu phan tu
	 * 2. Khong can khai bao truoc so luong phan tu (kich thuoc mang)
	 */
	
	public static void main(String[] args) {
		// 3 (ba) cach khai bao ArrayList
		ArrayList arrayListNone = new ArrayList<>();
		ArrayList<Long> arrayListPrimaryType = new ArrayList<>();
		ArrayList<Person> arrayListObject = new ArrayList<>();
		
		// No la class nen se co phuong thuc rieng cua no
		/*
		 * arrayList.size();
		 * 
		 * arrayList.add(Object o);
		 * arrayList.add(int index, Object o);
		 * 
		 * arrayList.set(int index, Object o);
		 * arrayList.get(int index);
		 * 
		 * arrayList.remove(Object o);
		 * arrayList.remove(int index);
		 * 
		 * arryList.isEmty();
		 * arrayList.contains(Object o);
		 * arrayList.retainAll(arrayListObject);
		 * 
		 * arrayList.indexOf(Object o);
		 * arrayList.lastIndexOf(Object o);
		 * arrayList.clear();
		 */
		
		
	}
}
